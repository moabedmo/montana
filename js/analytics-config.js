/** Set your Google Analytics 4 Measurement ID here (e.g. G-XXXXXXXXXX). Leave empty to disable. */
window.MONTANA_GA_ID = window.MONTANA_GA_ID || '';

/** Optional client override for Meta Pixel ID. Prefer NEXT_PUBLIC_FB_PIXEL_ID via /api/meta-public-config — leave empty (do not hardcode). */
window.MONTANA_FB_PIXEL_ID = window.MONTANA_FB_PIXEL_ID || '';
